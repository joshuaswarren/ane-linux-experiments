# TM recovery T8103 (jwm1): window aborted by second hard reset; -5 root cause scoped, mechanism still open

Date: 2026-09-16 (window work 04:29 -> 05:29 CDT)
Host: jwm1-linux (T8103, `ane@26bc04000`)
Branch: `joshuaswarren/omarchy-ane` `fix/tm-recovery`
Agent: AneTmRecoveryT8103

## Outcome

- **Acceptance NOT met.** No double provoke/recover on T8103, no exact
  smoke on 95dbcf3, **no main landing**. `origin/main` is unchanged at
  `6fa243ac7241`. The window was halted by the standing stop rule after a
  second hard reset; Main has the disposition call.
- `fix/tm-recovery` tip per Main's disposition is now **`b03f4b2`** =
  `b52064c` + a README branch note (force-pushed): T8103-safe recovery
  semantics only. The T6001-motivated commits (`dcc3e5b`..`327fd12`,
  including the set0/base gate, ACTUAL poll, pre-raise ordering and the
  T8103 ps-map guard `327fd12`) live on the new branch
  **`fix/tm-recovery-t6001`** for the jw16 lane. Neither tip is
  hardware-proven at its head: T8103 crashed post-guard (see Hard reset
  2), T6001 was never re-run at `327fd12`.
- jwm1 ended healthy: service `6fa243a` loaded (`3D83D6E0B587342305BE5C0`),
  refcnt 0, boot `44b8807e`, untouched after the crash.

## Hard resets (two, both in my lane, both on branch-family modules)

1. **04:50, boot `c2c36f6d` -> `f9deea55`.** During the deadbeef -110
   provoke on `95dbcf3` (srcversion `34EB6DCD2603828DF170794`), inside the
   25 s worker window. No panic lines, volatile journal lost the tail
   (journal ends 04:48:06, 2.5 min before the reset), no pstore on jwm1,
   `/var/tmp` window writes lost (dirty reset). The identical provoke was
   proven clean on this host with `b52064c` (2026-09-15 receipt), so the
   module delta was the variable.
2. **~05:27, boot `d68c3f14` -> `44b8807e`.** During phase B1 of the -5
   root-cause window: the instrumented guarded module `327fd12` + logging
   (srcversion `2EDC6BDA480BFBE4B32B7E3`) was loaded, the fusionon leg had
   started, and the box died before any TMINSTR/TMDUMP line reached disk.
   Volatile journal again kept only the first 2 s of the boot. `last`
   shows the boot vanished without a shutdown entry (crash signature).

**Upgraded finding.** Reset 2 removed the set0/base gate from suspicion:
the guard was live and the box still died. Common factor of both resets is
the branch's **recovery path after the genpd cycle**, whose post-cycle
engine re-init steps b52064c's proven-clean T8103 recovery lacked:
- the 8-queue `TQ_NID1 bit0` / `TQ_STATUS` clear (`95d3062`), and/or
- the `TQ_EN |= 0x3000` rewrite in `ane_tm_enable` (`f3ad6e5`).
One or both appear to external-abort T8103 when executed after the
partition cycle. Meanwhile the same fusionon workload on main `6fa243a`
only wedges gracefully (phase A), and `b52064c` survived two -5 recoveries
on this host (2026-09-15/16 journal). Consequence: merging
`fix/tm-recovery` as-is would put a T8103-lethal recovery on every T8103
box. T8103-safe recovery semantics = b52064c's exact post-cycle sequence.

## The -5 root-cause lane (Main's added scope)

Established, all code-verified on `origin/main` artifacts:
- Chain: runner (vulkan_encoder) -> resident client (`ane_resident.py`,
  byte-identical to repo, md5 `ef3770cba376bb3a4f12d0d4fef00109`) ->
  worker (`mlx-omarchy-ane-worker`, `AneWorker::submit` ->
  `execute_plan`: send memcpy -> `ane_exec` ioctl -> read memcpy,
  strictly sequential, exec blocks until the kernel sees
  `finished==3 && idle`) -> `libane` (`__ane_send` = memcpy into BO
  mmap, `DRM_IOCTL_ANE_SUBMIT` = doorbell; no cache maintenance, same as
  upstream) -> kernel `ane_tm_execute`.
- `-5` = `-EIO` from `ane_tm_execute`: entry-collect EVTC>64,
  poll-collect EVTC>64, or NID mismatch. `finish lines=0` every time.
- Inputs are host-synced before shipping (`mx.eval` + `np.asarray`).
  Phase A payload sha dumps prove L00-A/B/C rounds completed and the
  L01-A round's inputs were fully shipped when the -5 fired
  (`/var/tmp/AneTmRecoveryT8103/payloads-A/`, 13 tensors).
- Fusion changes only the GPU dispatch stream between worker rounds
  (chain+bias(+silu) fused; kill-switch `MLX_OMARCHY_CHAIN_FUSION`);
  bundles, worker, libane, client are byte-identical across arms.
- Determinism: fused 4/4 wedges across four boots on two module families;
  stock 0/5 (EncoderChainKernel) plus clean on every earlier run.
- Working hypothesis (untested): the next island submit lands inside the
  previous task's hardware retirement tail; the shorter fused GPU stream
  shrinks the L00-C -> L01-A gap below the hazard window. The
  `settle_us` module-param experiment (hold `engine_lock` N us after a
  completed submit) was built but never ran.

## Instrumentation staged for the next window

All throwaway, on jwm1:
- `/var/tmp/AneTmRecoveryT8103/window2.sh`: phaseA/B1/B2/B3/Z.
- Instrumented module source diff: `/tmp/branch-ane_tm.c` on the
  workstation, applied to `~/src/omarchy-ane-tmrec-t8103/ane/src/ane_tm.c`
  (worktree at 327fd12). Build: srcversion `2EDC6BDA480BFBE4B32B7E3`.
  Logs per submit (gap_us), the exact -EIO sub-path with EVTC counts and
  four FIFO INFO samples, TM_STATUS/COMMITTED/ERROR1..3 and queue words
  at every failure, completion poll_us; `settle_us` module param.
- Payload-dump runner copy: `/var/tmp/AneTmRecoveryT8103/fusion/`
  (env `TMINSTR_PAYLOAD_DIR`, sha256 per input tensor per round).
- Next window needs, in order: early serial console or a pstore that
  survives (see below), then B1 (settle=0, anatomy), B2 (settle=2000,
  fix validation), B3 (stock control), Z (restore + 64-el exact smoke).

## pstore armed (prep authorized by Main; no ANE work involved)

- jwm1 kernel has `CONFIG_PSTORE_RAM=m` but `CONFIG_PSTORE_CONSOLE=n`:
  ramoops captures **panic/oops dmesg dumps only, not a console mirror**
  -- and an external-abort death may never panic. Best available without
  a kernel rebuild; the journal tail loss problem remains open until a
  console-grade capture (serial/uartdbg) exists.
- Config: `/etc/modprobe.d/ramoops.conf` = `options ramoops
  mem_address=0xbd2c64000 mem_size=0x1a0000 console_size=0x180000
  record_size=0x20000 dump_oops=1`, `/etc/modules-load.d/ramoops.conf` =
  `ramoops`. The carve is the tail of the firmware-reserved nomap
  `isp-heap` region (0xbd2c64000+0x1ac000); this headless box never
  streams the camera. Both files are the whole install; delete to revert.
- Verified live: `ramoops: using 0x1a0000@0xbd2c64000` at 2.5 s uptime
  (early load confirmed after a config-activation reboot, one clean
  reboot, announced to Main).

## Branch state

- `fix/tm-recovery` = `b03f4b2` (`b52064c` + README branch note,
  force-pushed over `327fd12`). Compile-checked at `327fd12` on jwm1
  (0 errors/warnings, apple SDK kernel dir); the README note is docs
  only. NOT hardware-proven at either head: T8103 crashed post-guard
  (recovery path), T6001 was never re-run past `95dbcf3`.
- `fix/tm-recovery-t6001` = `327fd12` (new branch, pushed): the full
  T6001 chain `dcc3e5b`..`327fd12` above `b52064c` for the jw16 lane.
- `jwm1-ane.service` retarget: **NOT done** -- acceptance unmet. The
  service still loads `~/src/omarchy-ane-lifecycle-rebase/ane/ane.ko`
  (6fa243a), which is also what jwm1 runs now.

## Evidence files

- jwm1 `/var/tmp/AneTmRecoveryT8103/`: `window.log` (lost to reset 1),
  `phaseA.log`, `payloads-A/*.sha`, `fusion/` runner copy,
  `window2.sh`, instrumented module at
  `~/src/omarchy-ane-tmrec-t8103/ane/ane.ko` (`2EDC6BDA...`).
- Dead-boot journals: `c2c36f6d` tail lost; `d68c3f14` kept 2 s
  (early boot only). PMGR SET block survey (all 7 words `0x3ff`, powered):
  this transcript, 04:41 CDT.
- Pre-reboot -5 signature on b52064c (04:23:40 submit + 04:25:38 sysfs
  reset retry, both "tm not idle after reset: 0x0"): journal pull in this
  transcript.

## Not done

- Double provoke/recover on T8103 (acceptance) -- blocked by reset 1.
- Main landing -- blocked; branch recovery is T8103-lethal per finding.
- Service retarget -- blocked with the landing.
- settle_us experiment + -5 anatomy (B1/B2/B3) -- window burnt before B1
  evidence reached disk.
- T6001 re-proof at 327fd12 -- untouched, jw16 not in scope this lane.

## Disposition executed (Main, 2026-09-16)

- Lane STOPPED: no further ANE windows from this lane.
- Branch move: `fix/tm-recovery` force-pushed back to `b52064c` + note
  commit `b03f4b2` (README "Branch note: fix/tm-recovery is held at
  b52064c for T8103"); later commits preserved on
  `fix/tm-recovery-t6001` (tip `327fd12`). `origin/main` untouched at
  `6fa243ac7241`.
- jwm1 final state verified: service `jwm1-ane.service` active, module
  `6fa243a` / `3D83D6E0B587342305BE5C0`, refcnt `0`, boot `44b8807e`.
- Receipt handed to LandHostLeaves.
