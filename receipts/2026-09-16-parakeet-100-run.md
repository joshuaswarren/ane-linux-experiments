# 100/100 warm Parakeet E2E runs on the v0.5.1 release wheel: every run holds both pins — criterion 14 gate 10 closed (2026-09-16)

Verdict: **GATE 10 CLOSED.** 100/100 warm fused-E2E invocations on jwm1 with
the v0.5.1 release wheel site `/var/tmp/V051E2E/site` (`612eddc9` build,
non-diag) — every run **status=match, 104/104 emissions**, transcript sha
**`db501a8c…`** and `encoder_hidden` sha **`38c73261…`** asserted by file
sha256 on **all 100 runs** (one distinct hash each — zero drift), island
health **1 batch submit / 1 worker start / 0 timeouts on every run**,
`control: gpu-loop`, `tdt_fallback_reason: null`, `cpu_tensor_events` 0,
mel bit-exact, encoder bounds PASS. **No divergence occurred** — the
stop-and-capture protocol was armed (checker exits 42, run dir preserved,
no retry) but never fired. The staged 3-run proof
(`/var/tmp/V051E2E-battery/out-1..3`) was re-verified byte-exact against
both pins before launch.

## Configuration and runtime identity

Derived byte-for-byte from the proven `/var/tmp/V051E2E/battery.sh`
(3/3, pins held); only the loop count, per-run gate check, and RSS sampling
were added (`battery100.sh` sha `506f54b2…`, checker `7a3518d8…`).

| Component | Value |
| --- | --- |
| Wheel | `mlx_omarchy-0.32.2.dev202609161632+612eddc9-cp314-cp314-linux_aarch64.whl`, 7 772 610 B, sha `9c9f2835…` (`V051PIN.whl` = same bytes; direct_url matches) |
| Site | `/var/tmp/V051E2E/site` |
| Harness | `/var/tmp/ParakeetE2EBaseline7d82/fused_e2e.py` sha `0e38e7b1…` (same bytes as every prior green battery) |
| Runner | `/var/tmp/E2EREV/overlay/tools/coreml/vulkan_encoder.py` sha `eedb7c48…` — the exact runner the 3/3 release-wheel proof used |
| Worker | standalone fd-protocol `mlx-omarchy-ane-worker` sha `f171a61e…` (asserted per run) |
| libane | `libane-strict.so` (omarchy-ane `6fa243a` strict-bind) sha `56b46234…` (asserted per run) |
| libmlx | sha `a0bef352…`, mlx `0.32.2.dev202609161632+612eddc9` (asserted per run) |
| Islands | `island-reexport` bundles, `ANE_ISLAND_MODE=resident-batch` |
| GPU lock | `/tmp/m1-gpu.lock`, `flock -w 900` per run, inode 35 held→freed, never stolen/unlinked; `LOCK-FREE-OK` after |

**Warm contract (recorded as asked):** the runtime's contract is **one
fresh process per run** — that is what `fused_e2e.py` has always required
(the ANE island worker is spawned per run; `worker_starts=1` per run), so
all 100 runs are fresh processes, not a 10-run batching. Warmth comes from
the shared SPIR-V cache (pre-seeded from the proven 3-run battery, so even
r1 was warm: 8215.9 ms — no cold-SPIR-V run in this battery) and from the
same boot as that battery (driver/GPU paths already exercised).

## Result (100 runs, 11:56:44–12:11:37 local, 893 s wall, uptime 20 558→21 450 s)

Gate tally — every gate on every run pass (per-run records in
`per-run.jsonl`): `status_match`, `emissions_104`, `sequence_exact`,
`transcript_pin`, `encoder_hidden_pin`, `encoder_bounds`, `mel_bit_exact`,
`island_health`, `gpu_loop`, `identity` — **0 fails in 1000 gate
evaluations**.

| Metric | median | min | max |
| --- | ---: | ---: | ---: |
| total_pipeline (all 100) | 8549.4 ms | 7024.2 ms (r74) | 8723.2 ms (r82) |
| total_pipeline (warm r2–r100) | 8549.6 ms | 7024.2 ms | 8723.2 ms |
| ANE exec, worker-side | 2625.9 ms | 1898.0 ms | 2737.4 ms |
| wall clock per run | 8.9 s | 7.4 s | 9.1 s |

Memory trend (4 Hz /proc sampling of the E2E python process, peak RSS per
run, runs 1/50/100): **1360.9 → 1335.3 → 1350.9 MB — flat, no growth
trend** (spread 25.6 MB ≈ 1.9%); per-run island worker process not
included in the sample.

## Wedge surveillance

dmesg ring (`dmesg-after.txt`, covers 07:26:59 onward — battery window
fully inside): **zero ane/tm lines in the battery window**; the last
ane/tm line in the entire ring is 07:29:26 — pre-battery
AneTmRecoveryBisect recovery testing (`tm completion failed: -110` at
07:29:24 and 07:29:26, 4.4 h before battery start, each ending `tm
recovered: idle, accepting work again`). `preserving resources until
reboot`: **0 occurrences**. ANE hardware and TM idle and silent through
all 100 runs; lock free at end.

## Artifacts

`receipts/2026-09-16-parakeet-100-run/`: `100-run-summary.json` (machine
summary), `per-run.jsonl` (100 gate records with both sha256s per run),
`battery.log` (harness output + per-run wall), `battery100.sh`,
`check_run.py`, `run-battery100.sh`, `rss-{1,50,100}.csv`,
`dmesg-after.txt`, and `out-1/ out-50/ out-100/` (full `e2e-report.json`
+ `transcript.txt` each). Host-side: `/var/tmp/V051E2E-battery100/` (all
100 out/scratch dirs kept).

## Not claimed

- No claim on jw16, macOS, other fixtures, or cold-start behavior (the
  SPIR-V cache was pre-seeded; no cold run exists in this battery by design).
- The min run (r74, 7024.2 ms) is treated as scheduler variance on an
  otherwise 8.4–8.7 s population, not as a performance claim.
- `63c1d3cf` not merged, not touched.
