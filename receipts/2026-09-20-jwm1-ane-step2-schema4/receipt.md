# jwm1 ladder Step 2: schema-4 add-mul re-earn through the pinned worker — PASS (2026-09-20)

Lane: Jwm1AnePlan Step 2 (Main-authorized bounded execution: tests + pinned
regenerated schema-4 add-mul run-once, deadline 5000 ms, iterations 2,
exclusive inode-27 lock, GPU owner concurrence). Stop-first-fault armed; no
faults hit. Scope: the September schema-4 smoke re-earned on the restored
stack — not the full islands/soak ladder (Steps 3-5 staged, gated).

## Verdict

**PASS.** Worker exit 0; internal gate `verified output y exact`; 2
iterations, released=2, elapsed 3 ms. Independent gates reported separately:
numeric PASS (0 float-unequal pairs), bits: 5/64 signed-zero pairs
(float-equal, bit-different) — never merged into a single claim.

## Identities (recorded before execution)

| artifact | value |
|---|---|
| worker binary | `mlx-omarchy-ane-worker` sha256 `944f2a86cea719c4c10f6cd1a08c4c6df50b001cac0e381c38ca1f26277920cf` — NEW pinned-source build (ane-v064-wt snapshot `3611cd59…`); the historical `5e315017…` binary is not in any owned archive |
| worker tests | `omarchy_ane_worker_tests` `b5b29caf…` — 16/16 cases, 199/199 assertions PASS on-device, before the run |
| libane.so | `1ab9d95d…` — single-file gcc build from the `f261a6c` worktree (ABI-1 core; matches the Sept schema-4 libane pin; `ane_bind.h` correctly absent) |
| bundle | `schema4-bundle/`: manifest `3cf0ccf1…`, program-0 `9a6a6a9a…`, program-1 `62595e4a…` — regenerated by `h13_package_to_bundle.py` and reproducing the fixture `source.json` payload pins EXACTLY (witness commits `aa688df6` + `b12b03f1` fetched from origin) |
| driver | guard v2 `ane.ko` `99e8b8b5…`, loaded non-persistent, wedged=0 |

## Execution (under exclusive lock, inode 27, held throughout)

```
mlx-omarchy-ane-worker --bundle schema4-bundle --libane libane.so \
  --deadline-ms 5000 --iterations 2 \
  --input a=run/a.bin --input b=run/b.bin \
  --expect y=run/y.bin --save y=run/y-device.bin
→ bundle name=schema4-add-mul-worker programs=2 driver_abi=1 graph=5584d0fd…
→ worker status=0 iterations=2 released=2 elapsed_ms=3
→ verified output y exact
→ exit 0
```

## Independent gates (my analysis, separate from the worker's own gate)

- Device output `y-device.bin` (`bade941d…`) vs pristine reference `y.bin`
  (`abd3b8c9…`): **0 float-unequal pairs** across 64 fp16 elements.
- Bits: 5/64 elements differ — reference `-0.0` (0x8000) vs device `+0.0`
  (0x0000) at positions 7, 21, 35, 49, 63 (the i%7 signed-zero pattern of
  the deterministic inputs); the other 123 bytes identical. The worker's
  internal "exact" therefore also treats signed zeros as equal (float16
  tensor equality) — stated, not hidden.
- Device time: 3 ms for 2 iterations.

## Harness fixes made during staging (recorded, none device-touching)

- fixture .bin files must live OUTSIDE the bundle dir (the loader rejects
  unlisted files) → moved to `run/`.
- constants/model files materialize into per-package scratch copies
  (packages 01/04 need `c.bin`/`weights.bin` at model-root).

## State after

wedged=0; module bound, loaded non-persistent (no /lib/modules copy, no
modprobe.d, no service); sddm/NM/sshd active; lock released at session end.
Host healthy-clear for the GPU worker's corrected rerun.

## Evidence

`evidence/worker-raw-and-hashes.txt`,
`evidence/journal-after-step2.log` (948 lines),
`evidence/y-device.bin` (`bade941d…`), `evidence/y.bin` (`abd3b8c9…`).

## Next (gated on Main)

Steps 3-5 (islands bundle → 100-run soak → Parakeet/Qwen3.8 ANE) per
[the ladder plan](2026-09-20-jwm1-ane-qualification-ladder-plan.md) — exact
pins already documented.
