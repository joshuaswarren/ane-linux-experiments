# v0.6.2 release receipt (companion)

mlx-omarchy `v0.6.2` cut from the certified branch
`f122644-ane-parity-encoder-fix` tip `044f297f` (main runner bytes
`4f93726b` + the three additive O-dispatch hunks = runner sha
`e93500d2`, certified on both hosts 2026-09-17; receipt commit
`45797777` landed after the tag, on the branch).
https://github.com/joshuaswarren/mlx-omarchy/releases/tag/v0.6.2
Full receipt in that repo (`receipts/2026-09-17-v0.6.2-release.md`).

| asset | bytes | sha256 |
|---|---:|---|
| `mlx_omarchy-0.32.2.dev202609171025+044f297-cp311-cp311-linux_x86_64.whl` | 8566747 | `d83f7323f679c7ef15caa9f78a4e08897a3d6488f2c9f9f294c465cdf2f1a0f0` |
| `mlx_omarchy-0.32.2.dev202609171025+044f297f-cp314-cp314-linux_aarch64.whl` | 8307978 | `b8e944ef2f93ad2c9e296672eb85a1f90edf93e4fc12251c49bfc41ee6e8cf71` |

- Builds: aarch64 cp314 on jwm1 (`/var/tmp/v062-wt`, worktree at
  `044f297f`, no GPU lock; wheel ships the ANE worker `cc1caa6e` with
  the batch serve grammar + positional fill, strict libane `56b46234`,
  and the three canonical bundles), x86_64 cp311 on the dev box
  (`DEV_RELEASE=1`); sha256 re-verified after scp;
  check-version-stability PASS on both.
- `verify-release-assets.py v0.6.2` → **VERIFIED**.
- Pinned decode ctx1024 on the release asset (jwm1, fresh venv,
  mlx-lm 0.31.3, `MLX_DISABLE_COMPILE=1`): decode 105.12 tok/s,
  prefill 1111.07 tok/s, generated-IDs digest `7da83f06ec9f001d` —
  the standing pin, bit-exact.
- CLEAN Parakeet on the release asset (jwm1, fresh venv + clean HOME,
  `gate-jwm1.sh` from the tag): 3x warm transcribe identical —
  transcript `db501a8c…` EXACT, `encoder_hidden` file basis
  `38c73261…`, 104/104, `cpu_tensor_events` 0, 1 batch submit / 1
  worker start / 0 timeouts per run. `GATE PASS`.

## R4 — resident-batch ABCO (the measurement the worker-pairing gap blocked)

The wheel closes the instrument gap: its worker has the
`batch DEADLINE_MS | batch-end` serve grammar AND the dlopen'd
positional-fill path input-first o-proj bundles need. One
measurement-only runner patch (preload the o-proj bundles into the
resident session when `O` is placed; inert at `PLACED=ABC`; patched
copy `ed8c7758`; shipped wheel bytes untouched). Worker, fill lib
(`04a17653`), and bundles (`bundles-sf`) identical to the cert arms.

| host | arm | encoder_ane wall_ms | ane exec_ms | submissions | rel_l2 | hidden pin |
|---|---|---:|---:|---:|---:|---|
| jwm1 | ABC | 7012.3 | 2635.2 | 1 | 0.023044 | `38c73261` |
| jwm1 | ABCO | 7391.4 | 2729.4 | 1 | 0.023514 | `ef6afd13` |
| jw16 | ABC | 4922.1 | 2511.1 | 1 | 0.023044 | `38c73261` |
| jw16 | ABCO | 5649.3 | 2786.3 | 1 | 0.023514 | `ef6afd13` |

All gates held on every arm: 104/104, transcript `db501a8c` EXACT,
bounds PASS, mel bit-exact, 0 timeouts, 0 `cpu_tensor_events`.
Placement cost amortized: **+379 ms wall / +94 ms exec (jwm1), +727 /
+275 (jw16)** — vs +1141 / +1302 ms in launch mode. Verdict: residency
recovers most of the per-island round-trip tax but o-proj placement is
still a net loss; `MLX_OMARCHY_PLACED=ABC` stays the default, `ABCO`
stays opt-in. (submissions=1 is the resident-session counter: one
session open serving all batch rounds.)

jw16 unlock protocol followed exactly: `sudo systemctl stop
llm-inference.service` first, `flock` on `/tmp/m1-gpu.lock` (inode 12,
never unlinked), restart + `is-active` confirmed at hand-back; window
coordinated with AneRoundtripLevers (module stock `96d5a88` throughout
our arms).

`63c1d3cf` not merged; non-ancestor asserted before every push of this
session. Receipt-only companion push to this repo.
