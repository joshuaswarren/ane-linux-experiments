# v0.6.4 published — release VERIFIED, both-host gates green (2026-09-17)

Companion to `mlx-omarchy` `receipts/2026-09-17-v0.6.4-release.md`
(pushed to `origin/main` as `2da5e052`). Same facts, same evidence —
wheels, hashes, both-host gate tables, live-submit rows, and the
SET-base derivation findings live there. This file records the
outer-repo artifacts and what differs from the v0.6.3 run.

## Drivers

`.local/v064-release/v064-release-gate-driver.sh` (jwm1) and
`.local/v064-release/v064-release-jw16-driver.sh` (jw16) — the v0.6.3
corrected drivers with the v0.6.4 URL/sha/out paths.

## What differs from v0.6.3

- jwm1's gate preflight correctly refused at 2 GiB free on /tmp (the
  4.8 GiB foreign `/tmp/swift-bin-aur` untouched, live FFN-lane scratch
  excluded with the owner's OK). Fixed non-destructively: `sudo mount
  -o remount,size=12G /tmp` (non-persistent), second run green.
- jwm1 first relaunch hit a `pgrep -f` self-match in my one-liner (the
  pattern matched my own ssh command line) — machine was idle; the
  clean relaunch ran green end to end.

## Results (both hosts GREEN)

| | jwm1 (T8103) | jw16 (T6001) |
| --- | --- | --- |
| downloaded sha256 | `a2ddd188…` | `a2ddd188…` |
| packaging gate | exit 0, 3x warm identical | exit 0 |
| transcript | `db501a8c…` ×3 | `db501a8c…` ×3 |
| ctx1024 decode | `7da83f06ec9f001d` 105.47 tok/s | `7da83f06ec9f001d` 159.16 tok/s |
| service | n/a | stopped→restarted `active` |

Release: https://github.com/joshuaswarren/mlx-omarchy/releases/tag/v0.6.4
(tag = `112c32c4`, untouched); `verify-release-assets.py v0.6.4`
VERIFIED; worker live with schema identity guard exit 0; live submits
on x86_64 CT, jwm1, jw16, macstudio all carrying the new fields.
