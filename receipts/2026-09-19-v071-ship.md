# v0.7.1 ship — serving-stall fix fast-follow — 2026-09-19

Lane: ReleaseShipV071. Verdict: **PUBLISHED.** Tag `v0.7.1` = `50eeb290`
on `mlx-omarchy` `main`; both wheels built from the tag commit,
aarch64 sha256 re-verified after transfer, `verify-release-assets.py`
gate GREEN against the UPLOADED bytes.

## Tag

- tag: `v0.7.1` (annotated) on `50eeb290` (merge: timeline-stall-fix)
- pushed: `git push origin v0.7.1` → `[new tag]`
- ancestry: `git merge-base --is-ancestor 63c1d3cf origin/main` exit 1
  at fetch; re-asserted against the tag pre-release (exit 1 = PASS)

## Release

- URL: https://github.com/joshuaswarren/mlx-omarchy/releases/tag/v0.7.1
- assets, sha256 verified on dev box, aarch64 re-verified after scp:

```
mlx_omarchy-0.32.3.dev202609190758+50eeb29-cp311-cp311-linux_x86_64.whl
11747f4650ed8745216f18ddf5b635efa5df3bf5ff7b89c0aa9aeae4c4b57020
mlx_omarchy-0.32.3.dev202609190758+50eeb29-cp314-cp314-linux_aarch64.whl
e536056bcb23d8d93121edc662cebb0c4b14b8670233c8f018ad54150b9b8d49
```

- x86_64 built on the dev box (CPython 3.11) from the main checkout at
  `50eeb290` (build log `/var/tmp/v071-x86-build.log`, hub process
  `v071-x86-build`, exit 0).
- aarch64 built on jw16 (CPython 3.14) from release worktree
  `/var/tmp/v071-jw16` at `50eeb290`; sha256 re-verified identical
  after transfer (`e536056b…`).
- `tests/test_release_assets.py` on the dev box: 1 passed.

## Gate output (verify-release-assets.py v0.7.1, against UPLOADED bytes)

Both wheels: PASS sha256, PASS version consistent
(`0.32.3.dev202609190758+50eeb29`), PASS build commit `50eeb29`
matches tag `v0.7.1`, PASS feature strings (control
`MLX_DISABLE_COMPILE` present, profile absent), PASS platform tags
`linux_x86_64` / `linux_aarch64`, commit string found in
`libmlx.so`/cmake/METADATA/RECORD. Summary:
**VERIFIED: every uploaded asset matches what the release claims.**

## Numbers in the release body (all from the fix receipt)

`receipts/2026-09-19-timeline-stall-fix-jw16.md` (merged in `50eeb290`):
root cause = mid-tape rope-offset host readback blocking on its own
async pass's event latch; fix = settle+synchronize+detach_event in the
scalar path plus QueueSubmit-failure reservation retire. Ladder on jw16
fixed wheel: probe_bg.py exit=0 x3; mlx_lm.server 0.31.3 batched
12/12 completions + 12/12 chat HTTP 200, thread_deaths=0; main-vintage
12/12 chat; oMLX 0.6.4 Ministral 6/6 (was 3/3 stalls); single-stream
control 143.6 tok/s (no regression, baseline 82-84); Parakeet E2E
104/104 transcript-exact; primitive 103/103, runtime 41/41. Same
honest chat-truncation and T8103 carry-forward notes as the receipt,
restated in the release body.

## Arm-runner pin

`ane-linux-experiments/scripts/v070-jwm1-arm.sh` `EXPECT_WHEEL_SHA`
default bumped to the v0.7.1 aarch64 sha (`e536056b…`); env override
retained. jwm1 runner now gates on the published v0.7.1 bytes.

## Do-NOT scope

- jwm1 / jw14m2 — not touched; jw16 GPU — wheel build only, no GPU
  window needed, Jw16BtrfsMine ESP work not disturbed
- package source — not modified (release only)
